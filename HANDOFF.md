# 蟻級單元(Ant-Grade Unit)專案交接文件

> **給下一個 AI**:讀完本文件你應能不翻任何舊對話,直接接手三個候選方向之一。
> 文件涵蓋:設計決策與理由、凍結規格、模擬器架構與符號約定、全部校定值、
> 四個實驗實測結果、驗屍紀錄(六條教訓+整合期六屍)、藍本變更紀錄、
> 已知地雷、待辦存量、以及與使用者的協作慣例。
> 撰寫日期 2026-08-22(前版 2026-08-15);所有數字皆為磁碟上的實測/現值。

---

## 0. 交接快照(先讀這段)

**專案一句話**:五台 NT$2,600 的「蟻級」機器人,在 3×2 m 室內場地上,只用
昆蟲等級的感官(64×48 全景視覺、光流、編碼器、單軸陀螺、觸鬚)完成導航——
不用光達、不建地圖、不用 ROS。桌機 Python + 天花板相機當「上帝視角」裁判。

**目前狀態(本版新增)**:硬體規格凍結、**尚未購料**。模擬器完成一次大型
整合:World/PanoCamera 架構拆分(前一輪 review 引入)已與 sim/brain 完全
調和並驗收至**與舊版位元一致**;兩座模擬-實機橋搭通——橋一光流去旋轉
(含藍本變更 #1)、橋二球鏡展開校正工具(LUT 已產出、閉環驗證通過);
四個實驗在新堆疊全數重跑,結論保持,**唯 exp4 舊結論翻案**(見 §6/§7)。
smoke_test 擴為五段。三個下一步選項(§9)使用者**尚未選擇**。

**環境重要警告**:`/home/claude` 每次任務會重置。**唯一真相來源是使用者
下載區的最新檔案組**。接手開機流程:

```bash
# 請使用者上傳最新 ant_sim(或逐檔上傳)
pip install numpy matplotlib scipy --break-system-packages
python3 smoke_test.py    # 五段全過 = 環境正常;第 2 段數字應對上 §5.6 表
```

任何時候改動 `brain.py` 的匹配程式碼,先重跑 smoke_test 驗符號。
第 4 段(打滑航向誤差)是隨機實現,量級對(數十度)即可,勿逐值比對。

**交付物清單(本版)**:`config.py` / `world.py` / `sim.py` / `brain.py` /
`smoke_test.py` / `mirror_calib.py` / `pano_lut.h` / `pano_lut.npy` /
`mirror_calib.png` / `experiments/exp1–4` / `out/`(fig1–4、exp1.npy、
exp3.npy)/ `README.md` / 本文件。`make_demo.py` 沿用舊版(已驗相容)。
`world_old.py` 僅為驗證產物,可刪。

---

## 1. 使用者輪廓與協作慣例

- 台北,會寫程式(Python/C++ 可),**沒碰過 ROS**(刻意繞開),預算敏感,
  報價一律 **NT$**。
- 慣例語言**繁體中文**。回覆風格:**粗體段落標題+散文**,不用零碎條列;
  數據用表格;誠實優先——壞結果照報,除錯過程稱「驗屍」且使用者喜歡看。
- 每輪結尾給 2–3 個具體的下一步選項。
- 專案哲學三條:**先模擬後買料**;**演算法禁讀真值**(§4 鐵律);
  **每個門檻都要附校定流程**。
- 使用者口頭禪:「模擬真實狀況」——雜訊參數必須對應實體零件的真實缺陷。
- 本版新增慣例:**架構 refactor 與參數改動分開處理/commit**(整合期驗屍
  的直接教訓,§7.5)。

---

## 2. 專案演進史(為何長成這樣;已否決方向別再提)

選型旅程:都市環境監測 → 街道巡測車 → 掛纜 SlothBot → 無痕野巡車 →
**仿生導航群集測試平台**。轉折點是使用者主動加碼三個限制:不用光達、
資源有限、參考生物導航。核心論證:撒哈拉箭蟻用約 25 萬個神經元在無地標
鹽漠繞行數百公尺後直線回巢——ESP32-S3 對此任務資源過剩,限制本身就是
這門學問的核心。

**已否決、別再提**:光達/SLAM、樹莓派、GPS、磁力計羅盤(室內鐵磁干擾)、
ROS(正式延後)、太陽能/防水、機上建圖(反 SLAM:視覺記憶不建座標地圖)。

---

## 3. 凍結硬體規格(蟻級單元 v1,未購料)

規格表、成本表(五台含場地 ≈ NT$15,000)、閱讀清單同前版,此處只列
**本版新增的三條規格審查旗標**(購料/機構設計前必看):

1. **相機裁切 ≥480 px**:mirror_calib 幾何推導顯示,240 px 裁切下整個
   30 列羅盤帶在球照上徑向只佔 **5.1 px**(ε=55° 處 0.05 px/列)——上緣列
   嚴重過取樣。OV2640 支援更大裁切,韌體展開負載照算仍在預算內。
   替代方案:下修 EL_TOP(需照 config 警語重推 COMPASS_ROWS、重跑門檻)。
2. **等效視點高度 = 14.8 cm**(球下緣附近),非 config 假設的 12 cm;
   非中心散佈僅 ±0.5 mm(遠景近似成立)。機構定案後由 mirror_calib 回填
   `CAM_H`。相機視點須在**旋轉軸上**(桅杆同軸公差),偏心會毒 RIDF 的
   旋轉不變性。
3. **考慮第三根中央觸鬚**:±34° 兩鬚間有正面盲區,最小圓筒(觸發半徑
   0.05 m)可溜過;或改用 MPU6050 加速度計(現閒置)做碰撞/堵轉偵測。
   另:光流 PAA5100 的**安裝側向偏移**自本版起為校定常數 `FLOW_MOUNT_Y`
   (地位同 TRACK),機構圖定案後回填,實機用 smoke_test 第 5 段校定。

---

## 4. 軟體策略與鐵律

- **機上** C++(PlatformIO);**桌機** Python 裁判;ROS 延後不用。
- **鐵律(憲法)**:`brain.py` 只准讀 `SensorPacket` 與影像,嚴禁真值。
  真值只給裁判。`sim.last_frame_valid`(關燈旗標)屬**裁判側**,brain
  不得讀——brain 用 conf 自己發現關燈。
- **電量=飢餓** 狀態機仍未實作(brain 尚未消費 vbat),列於待辦(§10D)。

**呼叫契約(本版新增;破壞任一條 = 靜默失真)**:
1. sim 每個 tick 必呼叫 `world.step(DT, rng)`;漏呼叫 = 光照凍結,
   症狀是「一切變好」(最難察覺的失真)。
2. `PanoCamera.render(world, x, y, θ, ω, rng)` 回傳 `(img, valid)` tuple。
3. 每封包先 `HeadingBank.on_packet` 再 `PathIntegrator.on_packet`。
4. 物理保證車不入圓筒(render 對筒內直接跳過該筒);sim 已含推出碰撞。

**藍本變更紀錄(brain.py;逐條記錄,因它是韌體)**:
- **#1(2026-08)光流去旋轉**:`PathIntegrator.on_packet` 打滑比對前
  `ds_flow = flow_dx + gyro_z·FLOW_MOUNT_Y·DT`。合法性:只用封包欄位+
  校定常數;韌體對應 pi.cpp 感測預處理第一行。效果:20 s 蛇行、12 seeds,
  2 cm 偏移使 PI 終點誤差中位 3.8→5.9 cm、假觸發 604→685;修正後兩者
  **精確回到基線**(3.8 cm / 605)。

**演算法階梯與狀態**:1–5 階 ✅ 模擬驗證(exp1–4 本版重跑);
6 環吸引子、7 費洛蒙 ⬜ 未做(裁判介面已預留)。

---

## 5. 模擬器 ant_sim 完整說明

### 5.1 架構與檔案地圖(本版更新)

```
config.py     # 單一參數源:凍結規格・雜訊・門檻(附校定法)+ 耦合警語
world.py      # World(狀態:幾何+光照 OU;step() 唯一改狀態入口)
              # PanoCamera(純函數觀測;參數建構時複製;可多台=階7伏筆)
              # skyline_at 用 searchsorted 取樣(與舊版位元一致,勿改 floor)
sim.py        # 物理+雜訊工廠+封包;七項修補(見 §7.5);裁判 gt_*
brain.py      # ★韌體藍本(變更紀錄見 §4)
smoke_test.py # 五段校定
mirror_calib.py / pano_lut.h / pano_lut.npy   # 橋二
experiments/exp1–4(exp3 一行相容修補;exp4 協定升級 n=5)
make_demo.py  # 舊版沿用(相容已驗)
```

拆分理由與代價:狀態/觀測分離讓「時間流逝」與「拍照」解耦(多 render
不多漂移、離線比對合法、階 7 多觀測者免開刀);代價是正確性從「自動
成立」變「呼叫協定」(§4)。**金幀迴歸測試**仍是待辦(§10D)。

### 5.2 座標與符號約定(★改匹配程式碼前必讀;與前版相同)

世界座標 x 右 y 上 θ 逆時針;全景欄 i = 車體方位 i·5.625°,0=車頭逆時針增;
列 0=+55°、列 47=−5°;**左轉 δ ⇒ 影像左移 δ/5.625 px**;`ridf` 回傳左轉
為正(smoke_test 第 1 段為它而生,+30° 應估 +30.9°);`norm()` 仿射不變。
本版驗證:新相機欄位約定與 brain 符號相容(閉環 +30° 測 +30.35°)。

### 5.3 節奏

DT=0.005(200 Hz);VISION_FPS=12;決策 5 Hz。`frame_ready()`/`grab_frame()`。

### 5.4 雜訊模型 ↔ 實機對照(config 現值)

前版表全部沿用,**三處更新**:

| 項目 | 本版 | 說明 |
|---|---|---|
| 光照 OU | σ=5%、τ=8 s(**首次真正如此**) | **勘誤**:舊實作漏乘 dt,實測僅 ±1.45%、τ≈0.62 s。修正後因 `norm()` 仿射不變,演算法統計幾乎不動(增益掃描表紋風不動即證);教訓③的歷史兇手劑量應改寫為 ±1.5% 快漂 |
| 動態模糊 | 拖影方向隨 ω 符號 | 舊版恆同向(bug);閘控擋掉多數情境,影響翻滾幀統計級 |
| 光流 | 新增安裝偏移 `FLOW_MOUNT_X=0.03 / FLOW_MOUNT_Y=0.02`(佔位) | 感測點速度 (v−ωy, ωx) ⇒ dx 污染 −ω·y;sim 產污染、brain 去旋轉(變更 #1);另:光流現量**約束後真實位移**(舊版頂牆/圓筒仍回報在動) |

### 5.5 brain.py API 一覽(=韌體模組表)

與前版相同,唯 `PathIntegrator` 行為欄補一句:flow_rej 開啟時**先去旋轉**
`ds_flow = flow_dx + gyro_z·FLOW_MOUNT_Y·DT`,再與 ds_enc 比對(門檻同)。

### 5.6 校定值總表(正規化單位;門檻值全部未變)

門檻:`COMPASS_ROWS=30`、`COMPASS_NEAR=0.60`(≈6 cm)、`NEST_LOCK_FULL=
0.060`(≈6 cm)、快照到家 `0.040`(仍寫死於 SnapshotHomer,§10D)、
`RIDF_CONF_MIN=0.22`、`COMPASS_CONSIST=±30°`、`GYRO_GATE=80°/s`(寫死)、
`FUSE_K=0.15`、`SLIPREJ_THRESH=0.0004`、`HOME_STOP_EST=0.03`、
`FLOW_MOUNT_Y=0.020`(新)。

量測曲線(新堆疊 smoke_test 實測;**與前版表逐格重合**,即位元一致驗收):

| 離參考點 | 帶域 min-SSD | 全圖 IDF | RIDF conf |
|---|---|---|---|
| 0 cm | 0.173–0.205 | 0.026 | 0.88 |
| 3 cm | 0.417–0.442 | 0.027 | 0.75 |
| 6 cm | 0.552–0.580 | 0.055 | 0.68 |
| 10 cm | 1.497–1.517 | 0.102 | 0.21 |
| 20 cm | 1.913–1.934 | 0.168 | 0.04 |
| 40 cm | 1.452–1.508 | 0.322 | 0.23 |
| 關燈 | — | — | 0.060(被 0.22 正確拒收) |

**兩條新註**:①帶域門檻**各向異性**——校定沿 (0.7,0.71) 方向,他向 6 cm
可量到 0.38–1.93;實機校定方向必須固定。②第 4 段打滑航向誤差是隨機實現
(本版 83.0°,前版 34.9°;`world.step` 插入改變了亂數流),同 seed **只能
統計比對**;可逐位元比的只有 sky_h 陣列與無雜訊幀。第 5 段(新):側向
偏移回歸,真值 20 mm 量回 18.4 mm(差額=刻度誤差,實機兩者同段量到)。

### 5.7 實機校定手冊(五段)

1–4 同前版(第 2 段加註**方向固定**);新增第 5 段:原地交替旋轉 15 s,
`flow_dx/DT` 對陀螺 ω 回歸,斜率 = −FLOW_MOUNT_Y,回填 config。

### 5.8 球鏡展開(橋二;mirror_calib.py)

球面反射閉式解 → ε(u) 表 → LUT(48×64 ← 球照像素)→ 輸出 `pano_lut.npy`
(float,雙線性)與 `pano_lut.h`(uint16 最近鄰)。閉環驗證:模擬世界
光線追蹤成假球照 → LUT 攤平 → 對解析全景,同位姿 RIDF **+0.17°**
(conf 0.94)、+30° 手性測 **+30.35°**,PASS。**韌體必須 4-tap 雙線性**
——最近鄰在薄環帶把次像素估計咬出 ~1.5° 偏差(實測 +31.52°)。
實機流程(寫在檔頭):拍球照 → 量輪廓圓心與 rim_px → f=rim_px/tan(u_max),
u_max=asin(R/D) 由機構圖 → 重跑生成;手性反了改 AZ_SIGN;棋盤精修待硬體。
幾何情報三條見 §3。

---

## 6. 四個實驗:協定與實測結果(2026-08 新堆疊;舊表作廢)

**exp1 中央覓食五趟**(協定同前;n=12;中位數 cm;D 含完整光流鏈路):

| 配置 | 趟1 | 趟2 | 趟3 | 趟4 | 趟5 | 結論 |
|---|---|---|---|---|---|---|
| A 純編碼器 | 63.8 | 63.3 | 48.4 | 107.4 | 101.0 | 打滑判死刑 |
| B +陀螺 | 4.3 | 11.1 | 18.5 | 15.1 | 16.1 | 溫漂逐趟複利 |
| C +巢錨定視覺 | 3.4 | 5.4 | 6.4 | 6.5 | 6.0 | 壓平在鎖半徑 |
| D C+光流 | 3.4 | 3.5 | 5.1 | 4.6 | 6.7 | ≈C(靠泊精度由鎖半徑決定) |

**exp2 航向時間線**(關燈 30–55 s):enc/gyro/fused 最大|誤差|° =
關燈前 31.3/1.8/2.9、關燈中 24.1/4.6/**2.1**、復燈後 59.8/9.4/**2.5**;
復錨 27/55/92/119 s。優雅退化與彈回重現。

**exp3 集水區**(71 起點、50 s 逾時):成功率 **30%**(21/71);分帶
0–0.5 m **69%**(9/13)、0.5–1 m 41%、1–1.6 m 11%、>1.6 m 14%;成功中位
**3.3 cm**。區域技能結論不變(帶差多在個位數樣本內)。

**exp4 循路(協定升級:每起點 5 seeds,圖畫中位表現)**:
路上 **5/5**、中位 3.0 cm(max 10.7);+14 cm **5/5**、13.2 cm(max 73.3);
−17 cm+20° **1/5**、70.6 cm。**翻案紀錄**:前版「三者皆達(n=1)」——
儀表顯示該起步 fam 從第一幀即 ~0.69(路上 0.065),重建**舊堆疊**同測
5 seeds 亦 **1/5**(且舊堆疊 +14 cm 僅 2/5,新堆疊反而更穩)。修正結論:
**循路是「路廊技能」**,與 exp3 的區域技能互為鏡像。消融:光照凍結
逐位不變 ⇒ 光照 OU 無罪(norm 仿射不變當庭作證)。

執行耗時(本容器):exp1 ≈166 s、exp3 ≈45 s、exp4 ≈2 分、exp2 秒級。

---

## 7. 驗屍紀錄

### 7.1–7.5 前版五條教訓(全部有效,照舊)

①小場地沒有遠方→巢錨定;②搜尋幾何打架→PI 閉環慢螺旋;③光照漂移→
光度正規化(兇手劑量勘誤見 §5.4);④準週期天際線→陀螺一致性閘+鎖巢
解耦;⑤反射死鎖→三相繞行。細節見前版或 README。

### 7.6 第六條(本版新增):n=1 不是結論

exp4 舊結論被單次幸運抽籤誤導;5 seeds 才現形。規則:涉及**邊界能力**
(集水區邊緣、路廊邊緣)的實驗一律多 seeds;fam 這類離線指標在起步當下
就能預告成敗,先看儀表再下結論。

### 7.7 整合期驗屍(review 合併,2026-08;六屍)

1. **config 參數蒸發+世界參數被改寫**:review 版 config 掉 22 個常數、
   改場地/圓筒/相機仰角/雜訊值。→ 以凍結規格值全數還原;教訓:refactor
   與參數改動**分開 commit**。
2. **`world.step` 無人呼叫**:光照凍結,症狀「一切變好」。→ sim 每 tick
   呼叫;此類「變好型失真」只有金幀/指紋測試抓得到。
3. **`skyline_at` 取樣一格之差**:searchsorted vs floor = 紙帶平移 3.9 cm。
   生成公式與 sky_h 位元相同;還原 searchsorted 後無雜訊幀**逐像素相同**。
4. **舊 OU 漏乘 dt**(§5.4 勘誤)。
5. **舊模糊不分 ω 方向**(已修)。
6. **舊光流在接觸事件說謊**(頂牆/圓筒仍回報位移)→ 改量約束後真實位移。
另:仲裁方法備忘——README/HANDOFF 的數字本身就是**指紋**(6 cm→0.60、
關燈 conf≈0.05、+8.7°@10 cm),無舊碼也能驗世界等價性。

### 7.8 除錯方法論(沿用+補充)

先開儀表再猜;「先成功後失敗」找累積狀態;「固定距離失敗」對照場地
幾何;全場 IDF 地圖快篩混疊;**消融要選理論上乾淨的開關**(光照凍結
逐位不變即結案);**仲裁靠重建對照組**(舊堆疊重測是 exp4 翻案的關鍵)。

---

## 8. 韌體移植路徑(ESP32-S3)

- **Core 0(200 Hz)**:heading/pi(含去旋轉一行)/reflex/PID。負載個位數 %。
- **Core 1(12–15 fps)**:取像 → **`pano_lut.h` 4-tap 雙線性展開** →
  norm → ridf/img_diff(esp-dsp `dsps_fft2r_fc32`,估 1–3 ms/幀)。
  **幀須配曝光當下的陀螺航向**(12 fps 下差 80 ms 就是比對 80 ms 前的世界)。
- **記憶體帳更正**:視圖以 **uint8 存 PSRAM(3 KB/張)**,載入路線時一次
  展開 float+FFT;`RouteFollower` Python 版持 float+FFT 常駐是模擬便利,
  route.cpp **勿逐行照抄記憶體布局**。
- 序列協定文件須註明:`flow_dx` 為**未去旋轉**原始值,修正在 pi.cpp。
- rolling shutter:快轉為傾斜畸變,陀螺閘控本就擋(閘控第五用途)。
- 模組對應同前版。第一里程碑:smoke_test 段 1–3+5 的韌體版。

---

## 9. 下一步三選項(使用者尚未選;附開工要點)

**A. brain.py → ESP32 C++ 韌體**:PlatformIO 骨架+模組切分;先 SensorPacket
序列協定與 200 Hz 主迴圈,再上視覺(`pano_lut.h` 已就緒)。無硬體可交付
可編譯專案+主機端模擬串流測試。

**B. 階 6/階 7 進模擬器**:階 6 = `th_fused` 換 N=16 環吸引子(Stone 2017),
直接用本版 exp1/exp2 當對照組;階 7 = 裁判端費洛蒙網格+封包加一欄,
多機重現雙橋實驗。裁判介面已預留。

**C. 球鏡模組硬體(軟體半邊已完成)**:剩 3D 列印件(球~40–50 mm、桅杆、
相機座,依 §3 三旗標調整:≥480 px 裁切、CAM_H 14.8 cm、同軸公差)+
實照精修(Hough 找圓、棋盤/已知圓筒)。mirror_calib.py 換上實照即用。

---

## 10. 附錄

**A. 實驗協定細節**:同前版;exp4 新增 seeds = int(start_y·1000)+k·100,
k∈0..4;`out/exp1.npy`=dict(配置→(12,5)),`out/exp3.npy`=(7,11)。

**B. 已知限制與小地雷**:剪影成像(無紋理/鏡反射/球面高光);編碼器缺
背隙與堵轉模式(打滑=輪動車不動 vs 堵轉=都不動,毒性方向相反,實機
校定段 4 會量到);觸鬚正面盲區;光流僅前向、刻度隨 squal 未模;
容器無 CJK 字型 ⇒ matplotlib 一律英文標籤;`nest_locked` 逐幀瞬時勿當
閂鎖;`last_conf` 在閘控/非 fused 下不更新(消費端勿單獨當關燈偵測器,
覓食走遠 conf 亦 ~0.07);實驗腳本靠 `sys.path.insert` 找上層模組。

**C. 交付物**:見 §0。

**D. 待辦存量(brain review 未清項;動工前逐項評估)**:
1. `SearchSpiral` ω 上限 1.6 rad/s > 閘控 1.40,內圈(r<8.6 cm)幾何需求
   超閘 ⇒ 最內圈視覺失明;修法:ω 夾 ≤1.2 或 r0 提至 ~0.10。可能直接
   影響 exp3 近帶成功率。
2. `SnapshotHomer` 四個門檻寫死(0.040/1.06/6 s/0.98)+ `GYRO_GATE` 寫死
   ⇒ 搬進 config(校定流程填不到)。
3. 到家判定單幀鎖存 ⇒ 連續 2–3 幀;翻滾後無靜默 ⇒ 可能連鎖翻滾
   (相隔 <0.2 s 的翻滾對可驗)。
4. `Forager` OU 雜訊隨 DT 而非 √DT ⇒ 行為層降頻時統計變形。
5. 打滑比對單步(5 ms)訊噪差 ⇒ 改 50–100 ms 滑動視窗。
6. `ridf` docstring「航向增量」實為絕對航向(參考幀=0°),翻 heading.cpp
   前改名。
7. `_ssd_curve` 的 ref 參數與快取不一致(現無害,快取必填或移除參數)。
8. 金幀迴歸測試(無雜訊 debug 相機、固定位姿、CI 比對)——防 §7.7 類
   事故再犯的一勞永逸解。
9. 電量=飢餓狀態機(brain 消費 vbat);MPU6050 加速度計用於碰撞/堵轉。
