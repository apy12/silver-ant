# 蟻級單元(Ant-Grade Unit)專案交接文件

> **給下一個 AI**:讀完本文件你應能不翻任何舊對話,直接接手三個候選方向之一。
> 文件涵蓋:設計決策與理由、凍結規格、模擬器架構與符號約定、全部校定值、
> 四個實驗實測結果、五次除錯驗屍、已知地雷、以及與使用者的協作慣例。
> 撰寫日期 2026-08-15;所有數字皆為磁碟上的實測/現值,非記憶轉述。

---

## 0. 交接快照(先讀這段)

**專案一句話**:五台 NT$2,600 的「蟻級」機器人,在 3×2 m 室內場地上,只用
昆蟲等級的感官(64×48 全景視覺、光流、編碼器、單軸陀螺、觸鬚)完成導航——
不用光達、不建地圖、不用 ROS。桌機 Python + 天花板相機當「上帝視角」裁判。

**目前狀態**:硬體規格已凍結但**尚未購料**。Python 模擬器(`ant_sim/`)完成,
演算法階梯第 1–5 階在誠實雜訊下全部驗證通過,四個實驗有實測數據,五個
系統性 bug 已驗屍歸檔。使用者最後收到三個下一步選項(見 §9),**尚未選擇**。

**環境重要警告**:`/home/claude` 每次任務會重置。**唯一真相來源是使用者
下載區的 `ant_sim.zip`**。接手時的開機流程:

```bash
# 1. 請使用者重新上傳 ant_sim.zip(或本文件所附的最新版)
unzip ant_sim.zip -d /home/claude/ && cd /home/claude/ant_sim
pip install numpy matplotlib --break-system-packages
python3 smoke_test.py        # 應看到:FFT==暴力法 OK、RIDF 符號左轉為正、門檻表
```

`smoke_test.py` 四段全過 = 環境正常。任何時候改動 `brain.py` 的匹配程式碼,
先重跑它驗符號。

**交付物清單**(已在使用者下載區):`ant_sim.zip`(完整原始碼+README+out/)、
`README.md`、`fig1_homing.png`、`fig2_compass.png`、`fig3_catchment.png`、
`fig4_route.png`、`demo_trip.gif`(230 幀,一趟覓食→歸巢→螺旋→鎖巢)、
本文件 `HANDOFF.md`。

---

## 1. 使用者輪廓與協作慣例

- 台北,會寫程式(Python/C++ 可),**沒碰過 ROS**(這是刻意繞開的坑),
  預算敏感,報價一律 **NT$**。
- 慣例語言**繁體中文**。回覆風格:**粗體段落標題+散文**,不用零碎條列;
  數據用表格;誠實優先——壞結果照報,除錯過程稱「驗屍」且使用者喜歡看。
- 每輪結尾給 2–3 個具體的下一步選項(prose 或單選 widget 皆可,別連續用 widget)。
- 專案哲學三條:**先模擬後買料**;**演算法禁讀真值**(見 §4 鐵律);
  **每個門檻都要附校定流程**(不能是魔術數字)。
- 使用者的口頭禪式要求:「模擬真實狀況」——雜訊參數必須能對應到實體零件
  的真實缺陷,否則他會挑戰。

---

## 2. 專案演進史(為何長成這樣;已否決方向別再提)

選型旅程:都市環境監測 → 街道巡測車 → 掛纜 SlothBot → 無痕野巡車 →
**仿生導航群集測試平台**。轉折點是使用者主動加碼三個限制:**不用光達、
資源有限、參考生物導航**。回應是整隊砍掉重練:旗艦機解散(光達、樹莓派
退場),全隊統一為極簡「蟻級單元」,天花板相機從輔助升格為唯一裁判。
核心論證:撒哈拉箭蟻(Cataglyphis)用約 25 萬個神經元在無地標鹽漠繞行
數百公尺後直線回巢、誤差以公分計——ESP32-S3 對此任務**資源過剩**,
限制本身就是這門學問的核心。

**已否決、別再提的方向**:光達/SLAM(換老師,不是降級)、樹莓派(功耗
成本過剩)、GPS(室內無用)、磁力計當羅盤(室內鋼筋鐵磁干擾,已被視覺
羅盤+陀螺取代;曾列選配後除名)、ROS(正式延後,「等哪天想要 rviz 再說」)、
太陽能/防水(室內平台)、機上建圖(反 SLAM 哲學:用視覺記憶不建座標地圖)。

---

## 3. 凍結硬體規格(蟻級單元 v1,未購料)

| 模組 | 選型 | 角色 | 備註 |
|---|---|---|---|
| 主控 | Seeed XIAO ESP32-S3 Sense | 雙核 240 MHz、8 MB PSRAM、含 OV2640 | Core0 即時層 / Core1 視覺 |
| 全景視覺 | OV2640 朝天 + **空心**不鏽鋼球鏡(桅杆架高) | 反射 360° 環景,韌體展開成 **64×48 灰階** | 球必須空心:實心鋼球 110 g 直接壓垮重量帳 |
| 光流 | PAA5100JE 朝下 | 蜜蜂式視覺里程計;離地 **22 mm**(其有效距 15–35 mm) | 與編碼器互為對照組,打滑時見真章 |
| 馬達 | N20 磁編碼減速馬達 ×2,1:100 | 差速驅動;編碼器 **700 counts/圈** | 驅動 DRV8833 |
| IMU | MPU6050,**只用 Z 軸陀螺** | 200 Hz;開機靜置 2 s 校零 | 定位:「可以沒有,但要裝」——後被模擬用數據翻案成必需(§7) |
| 觸鬚 | 琴鋼線+微動開關 ×2(左右前 ±34°) | 蟑螂觸角式碰撞感 | 黑暗中也管用 |
| 費洛蒙(選配) | TCS34725 顏色感測器 + UV LED 朝下 | 夜光地墊讀/寫 | 階 7 才需要 |
| 其他 | 電量 ADC、頂面 6 cm AprilTag、WS2812 | 電量=飢餓;AprilTag 給天花板相機 | |

**尺寸重量**:車體 Ø13 cm、桅高 15 cm、離地 22 mm、整機約 270 g。

**運算與記憶體帳**(說服使用者的關鍵數字):RIDF 每幀約 20 萬次運算,
15 fps 輕鬆;循路全庫比對約 400 萬次,5–10 fps 負載六七成;單張快照 3 KB,
20 航點路線 <100 KB,PSRAM 放得下十倍。Core0 即時層負載 <20%。

**成本**(NT$):單機 ≈2,600(主控 600 / 馬達+驅動 500 / 光流 400 /
IMU+觸鬚+燈 210 / 球鏡+桅杆 150 / 電池充電 300 / 車體 250 / 雜項 150);
費洛蒙模組 +250/台。共用設施 ≈1,800(天花板 webcam 800 / 充電巢 500 /
地墊 500);夜光地墊選配 +1,500。**五台全隊含場地 ≈ NT$15,000**。

**場地道具**:黑色圓筒地標數支(Cartwright & Collett 1983 原始實驗道具)、
場地四周一圈列印「山稜線」紙帶(螞蟻認天際線)、燈光固定化。

**閱讀清單**(只給三本):Barbara Webb 昆蟲機器人綜述(領域地圖)、
Baddeley et al. 2012 熟悉度循路(第 5 階原典,可直接照抄)、
Braitenberg《Vehicles》(第 1 階靈魂)。延伸:Zeil 2003(集水區)、
Wehner 搜尋螺旋、Stone et al. 2017(環吸引子上機器人)。

---

## 4. 軟體策略與鐵律

- **機上**:C++(PlatformIO),全部導航演算法在 ESP32 上跑。
- **桌機**:Python「上帝視角」——天花板相機 AprilTag 追蹤 = 真值裁判 +
  虛擬費洛蒙場 + 記錄繪圖。robots 經 WiFi/MQTT 上傳遙測。
- **ROS 延後不用**。
- **鐵律(整個專案的憲法)**:演算法層(模擬器中的 `brain.py`,未來的韌體)
  **只准讀 `SensorPacket` 與相機影像,嚴禁引用任何真值**。真值只給實驗
  腳本當裁判。這條鐵律是「模擬→韌體逐行對翻」可行的原因。
- **評分文化**:每一階都有天花板真值打分數;基準線是純里程 PI 與隨機遊走。
- **電量=飢餓**:飽→出巢覓食(沉積費洛蒙)、餓→回家向量返巢、到巢→充電。
  充電巢就是巢。

**演算法階梯與狀態**:

| 階 | 生物原型 | 實作 | 狀態 |
|---|---|---|---|
| 1 | Braitenberg 載具 | `Controller`(三相繞行反射)、`Forager`(OU 隨機遊走) | ✅ 模擬驗證 |
| 2 | 箭蟻路徑積分 | `PathIntegrator`+`Homer`+`SearchSpiral`(Wehner 搜尋) | ✅ 模擬驗證(exp1) |
| 3 | 視覺羅盤 | `ridf()` FFT 版 + `HeadingBank` 四重閘融合 | ✅ 模擬驗證(exp1/2) |
| 4 | 快照歸巢 | `SnapshotHomer` run-and-tumble 沿 IDF 梯度 | ✅ 模擬驗證(exp3) |
| 5 | 熟悉度循路 | `RouteFollower` 完美記憶版(Baddeley 基準線) | ✅ 模擬驗證(exp4) |
| 6 | 環吸引子航向 | 把 `HeadingBank` 換成神經元環(Stone 2017) | ⬜ 未做 |
| 7 | 群集費洛蒙 | 虛擬費洛蒙(裁判網格)→ 夜光地墊;雙橋實驗 | ⬜ 未做(裁判介面已預留) |

---

## 5. 模擬器 ant_sim 完整說明

### 5.1 檔案地圖

```
ant_sim/
├── config.py        # 全部物理/雜訊參數(§5.4 逐項對應實機)
├── world.py         # 3×2m 場地、5 圓筒、牆面稜線(SKYLINE_SEED=7)、全景成像
├── sim.py           # 200Hz 物理、打滑事件、SensorPacket、觸鬚、裁判介面
├── brain.py         # ★韌體藍本:演算法全在此,禁讀真值
├── smoke_test.py    # 校定工具(=未來實機校定腳本),四段自檢
├── make_demo.py     # 錄 GIF
├── experiments/
│   ├── exp1_homing.py     # 中央覓食五趟,四配置(~2.5-3 min)
│   ├── exp2_compass.py    # 航向時間線+關燈(秒級)
│   ├── exp3_catchment.py  # 快照歸巢集水區(~1 min)
│   └── exp4_route.py      # 循路回放(秒級)
├── out/             # fig1-4.png、demo_trip.gif、exp1.npy、exp3.npy
└── README.md        # 使用者版說明(內容為本文件子集)
```

### 5.2 座標與符號約定(★改匹配程式碼前必讀)

- 世界座標:x 右、y 上、θ 逆時針,自 +x 起算;`wrap()` 到 (−π, π]。
- 全景影像:**欄 i = 車體方位角 β = i·5.625°**,0=車頭、逆時針增;
  **列 0 = 仰角 +55°,列 47 = −5°**(48 列線性)。
- **左轉 δ ⇒ 影像內容向左平移 δ/5.625 px**(特徵欄位 =(世界方位−θ)/Δ)。
- `_ssd_curve(cur, ref)[s] = mean((roll_left(cur, s) − ref)²)`,由
  `irfft(rfft(cur)·conj(rfft(ref)))` 求圓周相關;次像素拋物線內插夾 ±0.5 px。
- **`ridf(cur, ref)` 回傳航向增量,左轉為正**(回傳處有 `-shift` 取負,
  這裡曾出過一次符號 bug——smoke_test 第 1 段就是為它而生,+30° 應估出
  +30.9°、誤差 <1°)。conf = (mean−min)/mean。
- `img_diff()` = 全部旋轉的最小 SSD(Zeil 的旋轉不變 IDF)。
- `RouteFollower`:模擬「左轉 s px」= `roll_left(img, s)`;`steer()` 回傳
  dth = s·5.625°,左轉為正,掃描窗 ±12 px = ±67.5°。
- `norm()` 光度正規化(零均值/單位變異)有仿射不變性:
  `norm(norm(x)[:rows]) == norm(x[:rows])`,故帶域可重複正規化不出錯。

### 5.3 節奏

物理/即時層 `DT=0.005`(200 Hz);視覺 `VISION_FPS=12`;導航決策 5 Hz。
`sim.frame_ready()` 控幀;`sim.grab_frame()` 取像(含光照/模糊/量化)。

### 5.4 雜訊模型 ↔ 實機對照(config 現值)

| 參數 | 值 | 實機來源與校定法 |
|---|---|---|
| 打滑事件 | 率 0.06/s + 0.5·\|ω\|;因子 U(0.35,0.8);持續 U(0.10,0.30)s | N20 橡膠輪急轉/加速;實機用天花板相機對照編碼器量。**編碼器量的是輪子轉動(馬達有轉就有數),打滑=輪動車不動 ⇒ 里程高估** |
| 微打滑 | 每步乘性 σ=0.6% | 滾動阻力常態變化 |
| 編碼器 | 700 counts/圈量化(0.153 mm/count) | 7 ppr × 1:100 |
| 陀螺 | 校零殘偏 σ=0.03°/s;**偏置隨機游走 σ=0.012°/s/√s(含溫漂)**;白雜訊 0.06°/s;刻度 ±1% | 開機靜置 2 s 校零;溫漂值是刻意調到誠實水準,讓長時程漂移現形 |
| 光流 | 刻度 ±3%(安裝高度);雜訊 2.5 mm/s;squal 掉線 0.02 事件/s、持續 0.2–0.8 s | PAA5100;深色地墊會掉 squal |
| 全景 | 像素雜訊 4/255;**全域增益 OU 漂移 σ=5%、τ=8 s**;8-bit 量化;動態模糊=等效曝光 1/30 s 的水平拖影 | OV2640+室內燈;增益漂移是教訓③的兇手 |
| 機構 | 輪 R=17 mm、輪距 100 mm、V_MAX=0.30、馬達一階 τ=0.08 s、車體 R=65 mm、觸鬚 reach=100 mm | |
| 場地 | 3.0×2.0 m;巢 (0.60,0.60);5 圓筒(座標見 config);牆稜線 base 0.32+amp 0.14,seed=7 | 稜線 seed 固定=「印出來就不會變」 |

成像模型:黑剪影/亮背景,取所有物體剪影**聯集**(對黑物體物理正確,
自帶遮擋);地板 0.48、背景 0.80、剪影 0.10;關燈=全圖 0.02+雜訊。

### 5.5 brain.py API 一覽(= 韌體模組表)

| 類別/函式 | 介面 | 行為要點 |
|---|---|---|
| `norm(img)` | → img | 匹配前必經 |
| `ridf(cur, ref, ref_fft, ref_sq)` | → (dθ, conf, dmin) | FFT 圓周相關,0.07 ms/次(Python) |
| `img_diff(...)` | → min-SSD | 旋轉不變 IDF |
| `HeadingBank(mode)` | `on_packet(p)`、`on_frame(img)`、`.theta`、`.nest_locked` | mode ∈ enc/gyro/fused。fused=陀螺傳播+視覺錨定。**四重閘**:conf≥0.22、帶域 dmin≤0.60、\|gyro\|≤80°/s、與現行估計一致 ±30°。`nest_locked` 為**逐幀瞬時**旗標(全圖 IDF<0.060),與融合閘**解耦** |
| `PathIntegrator(hb, use_flow_rej)` | `on_packet`、`reset()`、`home_vector()`、`dist()` | ds=編碼器均值;flow_rej 開啟時 \|ds_enc−ds_flow\|>0.0004 m/步 ⇒ 改信光流 |
| `Controller(rng)` | `mix(pkt, v, ω)` → (vL, vR) | 觸鬚三相:BACK 0.35–0.55 s(v=−0.10, ω=±2.2 轉開)→ SIDE 0.9–1.5 s(v=0.14 直走破對稱)→ 還控制權 |
| `Forager(rng)` | `act()` | OU 轉向隨機遊走,v=0.20,ω 夾 ±1.5 |
| `Homer(pi)` | `act()` → (v, ω, done) | P=2.5 夾 ±2.0;v=0.18·max(0.15,cos e);\|p̂\|<0.03 ⇒ done |
| `SearchSpiral(pi, v=0.12, r0=0.05, growth=0.008)` | `act()` | **PI 閉環** Wehner 螺旋:在 PI 座標繞相信的巢畫擴張圈;伺服 P=3.0 夾 ±1.6 |
| `SnapshotHomer(rng, snapshot)` | `on_frame`、`act()`、`.arrived` | run-and-tumble:與**本段航程最佳值**比,惡化 >6%(MARGIN=1.06)才翻滾(0.30–0.60 s @±1.8–3.0 rad/s);停滯 6 s 大翻滾逃平原;run v=0.17;到家門檻 D<0.040 |
| `RouteFollower(views)` | `steer(img)` → (dθ, fam) | 完美記憶:min over(視圖×旋轉)SSD;FFT 預算視圖庫 |

### 5.6 校定值總表(★全部為**正規化單位**,舊 raw 值已作廢)

門檻:`COMPASS_ROWS=30`(上半帶,仰角>~17.5°)、`COMPASS_NEAR=0.60`
(錨定半徑≈6 cm)、`NEST_LOCK_FULL=0.060`(鎖巢≈6 cm)、快照到家 `0.040`
(≈4–5 cm,寫死在 SnapshotHomer)、`RIDF_CONF_MIN=0.22`、
`COMPASS_CONSIST=±30°`、陀螺閘控 `80°/s`(HeadingBank.GYRO_GATE)、
`FUSE_K=0.15`、`SLIPREJ_THRESH=0.0004`、`HOME_STOP_EST=0.03`。

量測曲線(增益 0.92–1.08 掃描下紋風不動;smoke_test 第 2 段可重現):

| 離參考點 | 帶域 min-SSD | 全圖 IDF | RIDF conf |
|---|---|---|---|
| 0 cm | 0.18–0.21 | 0.026 | 0.88 |
| 3 cm | 0.41–0.43 | 0.027 | 0.76 |
| 6 cm | 0.52–0.57 | 0.055–0.056 | 0.68–0.69 |
| 10 cm | 1.51–1.55 | 0.104 | 0.20 |
| 20 cm | 1.90–1.93 | 0.169 | 0.03–0.04 |
| 40 cm | 1.49–1.57 | 0.32 | 0.19 |
| 關燈 | — | — | 0.045–0.058(被 0.22 正確拒收) |

其他實測:編碼器 20 s 混合行駛航向誤差 **34.9°**(打滑殺傷力);
全場 IDF 地圖**無混疊**(<0.06 且離巢 >15 cm 的點數 = 0,全域最小值
恰在巢);`ridf` 帶域 0.07 ms/次且與暴力法逐位元相等(atol 1e-10)。

### 5.7 實機校定手冊(smoke_test 四段的實機版)

1. 機器人置於巢、面向 0°:原地轉已知角度(±10/30/80°)→ 驗 RIDF 符號、
   精度、conf 正常值域。
2. 用尺做已知位移 3/6/10/20 cm → 量帶域 min-SSD 與全圖 IDF → 重填三門檻。
3. 關燈一次 → 確認 conf 掉到門檻下。
4. 混合行駛 20 s 對照天花板相機 → 量自家地墊的實際打滑,回填 config。

---

## 6. 四個實驗:協定與實測結果

**exp1 中央覓食五趟**(每趟覓食 25 s → PI 歸巢 max 40 s → C/D 螺旋搜尋
max 40 s → 巢口停頓 0.8 s;seeds 2000+,n=12;到家誤差=宣告靠泊瞬間與
真巢距離,中位數 cm):

| 配置 | 趟1 | 趟2 | 趟3 | 趟4 | 趟5 | 結論 |
|---|---|---|---|---|---|---|
| A 純編碼器 | 93 | 128 | 126 | 124 | 158 | 打滑判死刑 |
| B +陀螺 | 8.7 | 8.9 | 12.3 | 17.1 | 21.5 | 短期漂亮,溫漂逐趟複利 |
| C +巢錨定視覺 | 5.2 | 4.6 | 4.1 | 5.1 | 5.3 | **壓平**在鎖半徑 |
| D C+光流打滑抑制 | 5.0 | 4.7 | 6.0 | 5.5 | 3.8 | ≈C(見下註) |

註:C≈D 因靠泊精度由視覺鎖半徑決定;光流的價值在**著陸距離與搜尋時間**
(打滑重時),此指標未呈現。B 的逐趟惡化 = 當初「IMU 可以沒有但要裝」
之爭的數據答案;C 的平線 = 演算法階梯存在理由。

**exp2 航向時間線**(seed 7;短趟中央覓食;關燈 30–55 s;三個 HeadingBank
並行吃同一串封包,fused 控車):最大|航向誤差|° enc/gyro/fused =
關燈前 31.5/1.3/1.9、**關燈中 73.7/3.5/1.4(fused 靠陀螺優雅退化)**、
復燈後 130/15.2/**5.0(77 s 鎖巢瞬間彈回)**;復錨事件 77/96/120 s。
註:此腳本搜尋 20 s 就放棄(為時間線緊湊),故首鎖較晚,屬設計而非 bug。

**exp3 快照歸巢集水區**(71 有效起點、隨機初始航向、50 s 逾時、成功=
最終真距 <10 cm):總成功率 **35%**,但空間結構才是重點——起點距巢
0–0.5 m:**85%**(11/13)、0.5–1 m:47%、1–1.6 m:21%、>1.6 m:9%;
成功案例最終距離中位數 **3.5 cm**。結論:**快照歸巢是半徑約 0.8–1 m 的
區域技能**(重現 Zeil 2003),此即階 5 的存在理由。

**exp4 循路回放**(S 曲線 waypoints 見腳本;31 張視圖 @8 cm ≈ 93 KB;
決策 5 Hz,ω=clip(2.5·dθ, ±1.8),v=0.14):路上起步平均偏差 **2.4 cm**
(max 6.1);+14 cm 側偏起步 13.3 cm;−17 cm+20° 航向偏移起步 18.0 cm;
**三者皆達終點**(17–20 s)。

執行耗時參考:exp1 約 2.5–3 分、exp3 約 50 s、exp2/exp4 秒級、GIF 約 1 分。

---

## 7. 五條實戰教訓(驗屍紀錄;實機一定會再遇到)

1. **小場地沒有「遠方」→ 巢錨定架構**。症狀:視覺融合配置比純陀螺還爛
   100 cm+。機制:單一快照的絕對羅盤在 2×3 m 場地有效半徑僅 6–10 cm
   (位移 10 cm ⇒ +8.7° 誤差;20 cm ⇒ 垃圾)——平移視差是物理不是雜訊
   (真螞蟻用「天空」正因其無視差)。修法:放棄全域羅盤,**只在視覺確認
   到巢時錨定航向並把 PI 歸零**(螞蟻本來就在巢歸零積分器);實驗因此
   改成中央覓食模式。韌體含義:別在小場地追求全域視覺定向。

2. **搜尋幾何會跟自家感測防線打架 → 慢速 + PI 閉環 Wehner 螺旋**。症狀:
   著陸 10–15 cm 竟搜不到巢。機制之一:螺旋初速轉 183°/s 觸發自家 80°/s
   陀螺閘控,所有幀被擋;之二:壓速後最小迴轉半徑 14.5 cm > 著陸誤差,
   巢落在螺旋中央盲區且開環螺旋會漂移。修法:搜尋降速 0.08→迴轉 7 cm、
   歸巢途中鎖定即靠泊、最終版改 **SearchSpiral:在 PI 座標繞「相信的巢」
   畫擴張同心圈**——打滑再多 PI 都把圈拉回中心(沙漠螞蟻的搜尋幾何)。

3. **光照增益漂移 ±5% 淹掉完美燈況校定的門檻 → 光度正規化**。症狀:
   「第一趟成功、之後全滅」(前 60 s 燈還沒漂遠)。修法:所有匹配前
   `norm()`(零均值/單位變異),三個門檻改用正規化單位重校(§5.6 表)。
   這是視覺歸巢文獻的標準配備,實機必裝(自動曝光關掉也有殘餘漂移)。

4. **牆面天際線準週期 → 自信但大錯的角度 → 陀螺一致性閘**。症狀:一趟
   覓食內航向被毒到 +47°,PI 跟著毒出「幽靈巢」,之後每趟固定回幽靈巢
   繞圈(~50 cm 永久誤差)。機制:牆邊某些點帶域 dmin 意外 <0.60 且 conf
   過關,但最佳旋轉是混疊假角度;FUSE_K=0.15@12fps 一兩秒就把航向拉走。
   修法:**視覺角度須與現行估計一致 ±30° 才融合**(混疊假角度通常差幾十
   到百餘度,直接被擋)——陀螺的又一用途;同時**鎖巢判定(旋轉不變)從
   融合閘解耦**,角度垃圾不陪葬。

5. **P 控制器 + 單純反射 = 圓筒死鎖 → 三相繞行**。症狀:歸巢逾時,著陸
   點距離恰為各圓筒位置(0.91/1.6 m…)。機制:觸鬚反射轉開 57°,P 控制器
   立刻轉回去撞,無限循環。修法:反射改三相(倒退轉開 → **直走 1 秒破
   對稱** → 還控制權)。

**除錯方法論**(對下個 AI 的建議):先開逐趟/逐種子儀表(鎖定途徑、著陸
距離、搜尋耗時、航向誤差、假鎖計數)再猜;「先成功後失敗」找累積狀態;
「固定距離的失敗」對照場地幾何(圓筒/牆/幽靈巢)。全場 IDF 地圖是排除
混疊的快篩。

---

## 8. 韌體移植路徑(ESP32-S3)

- **Core 0(200 Hz)**:`HeadingBank.on_packet`、`PathIntegrator.on_packet`、
  `Controller.mix`、輪速 PID、安全。每步幾十次浮點,負載個位數 %。
- **Core 1(12–15 fps)**:取像 → 查表展開 64×48(展開 LUT 由球鏡校正產生,
  見 §9C)→ `norm` → `ridf`/`img_diff`。FFT 用 esp-dsp `dsps_fft2r_fc32`
  (64 點 × 48 列);Python 0.07 ms/次 ⇒ ESP32-S3 估 1–3 ms,15 fps 綽綽有餘。
- **記憶體**:視圖 3 KB/張;循路 31 張=93 KB;PSRAM 8 MB 可放 80 倍路線。
- 模組對應:`SensorPacket`=序列上行封包結構、`HeadingBank`→heading.cpp、
  `PathIntegrator`+`SearchSpiral`→pi.cpp、`SnapshotHomer`→snapshot.cpp、
  `RouteFollower`→route.cpp、`Controller`→reflex.cpp。因鐵律,逐行對翻即可。
- 第一里程碑:桌上跑通 smoke_test 的韌體版(段 1–3),門檻用實機重校。

---

## 9. 下一步三選項(使用者尚未選;附開工要點)

**A. brain.py → ESP32 C++ 韌體**:PlatformIO 專案骨架 + 上述模組切分;
先做 SensorPacket 序列協定與 200 Hz 主迴圈,再上視覺。無硬體也能交付
可編譯專案 + 主機端模擬串流測試。

**B. 階 6/階 7 進模擬器**:階 6 = 把 `th_fused` 換成 N=16 神經元環吸引子
(陀螺=活動平移輸入、視覺=bump 校正;Stone et al. 2017 的 CX 模型),
直接重跑 exp1/exp2 對照;階 7 = 裁判端費洛蒙網格(沉積/蒸發/擴散),
`SensorPacket` 加一欄虛擬費洛蒙讀值,多機模擬重現**雙橋實驗**(最短路徑
湧現)。裁判介面已在架構中預留。

**C. 全景球鏡模組硬體**:空心不鏽鋼球(~40–50 mm)+桅杆+相機座的 3D
列印件,加**展開校正程式**(棋盤/已知圓筒法產生 極座標→64×48 的 LUT,
輸出 C 陣列給韌體)。這是實機視覺品質的第一瓶頸。

不論選哪個,回覆風格照 §1 慣例;若進入購料,BOM 以 §3 成本表為準。

---

## 10. 附錄

**A. 實驗腳本協定細節**:exp1 Forager 種子 = seed·31+trip;exp3 網格
x∈linspace(0.25,2.75,11)、y∈linspace(0.25,1.75,7),距圓筒 <r+0.13 的
起點跳過,seed=300+j·20+i;exp4 waypoints=[(0.45,0.50),(1.00,0.42),
(1.50,0.50),(1.95,0.75),(2.00,1.10),(2.20,1.35),(2.60,1.30)](已驗證
與圓筒淨空 ≥0.14 m)。`out/exp1.npy`=dict(配置名→(12,5) 誤差陣列,
allow_pickle);`out/exp3.npy`=(7,11) 最終距離。

**B. 已知限制與小地雷**:成像是剪影模型(無紋理/鏡反射);光流只模前向
分量;容器無 CJK 字型 ⇒ **matplotlib 圖一律英文標籤**(中文會變方框),
後端用 Agg;`brain.py` 的 SnapshotHomer 到家門檻 0.040 是寫死的(沒進
config,重校時別漏);`nest_locked` 是逐幀瞬時旗標,別當閂鎖用;實驗
腳本靠 `sys.path.insert` 找上層模組,搬目錄要留意;GIF 用 PillowWriter
(Pillow 已裝)。

**C. 交付物**:ant_sim.zip / README.md / HANDOFF.md(本文件)/
fig1_homing.png / fig2_compass.png / fig3_catchment.png / fig4_route.png /
demo_trip.gif。zip 內含本文件與 README 各一份。
